
entities["mining-robot"] = {
	build = function(entity)
		local forceName = miningForceForEntity(entity)
		entity.force = forceName
	end
}

	entities["mining-robot-2"] = {
		build = function(entity)
			local forceName = miningForceForEntity(entity)
			entity.force = forceName
		end
	}

	entities["mining-robot-3"] = {
		build = function(entity)
			local forceName = miningForceForEntity(entity)
			entity.force = forceName
		end
	}

	entities["mining-robot-4"] = {
		build = function(entity)
			local forceName = miningForceForEntity(entity)
			entity.force = forceName
		end
	}

	entities["mining-robot-5"] = {
		build = function(entity)
			local forceName = miningForceForEntity(entity)
			entity.force = forceName
		end
	}